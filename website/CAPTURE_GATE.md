# Real Windows UI capture gate

The marketing site intentionally contains no product screenshot yet.

`docs/assets/agent-world-ui.png` depicts the retired spatial grid and must never be used as evidence of the current interface. The current source renders a list-first operator panel beside a selected timeline, prompt composer, and installed-provider readiness disclosure.

The source now includes one globally admitted, final-result-only Codex path for an eligible, reverified worktree. It requires exactly `codex-cli 0.146.0`, a reviewed read-only/no-approval feature/config preflight, and durable lifecycle and provider-session correlation with no automatic replay after restart uncertainty. This is a source-and-fixture claim. No authenticated model turn or authoritative real-Windows sandbox, read-scope, network, process-tree, or reopened-SQLite evidence bundle has been run for promotion.

Its worktree is the working-directory and Git-state boundary, not a worktree-only read or host-secret boundary. Codex may read other user-readable files; the prompt and model-selected readable context go to the configured Codex service. `apply_patch` may remain model-visible, while writes are expected to be denied and any `file_change` event fails the turn.

A screenshot may replace the capture-gate notice only when all of these checks pass:

- Capture the executable built from the current `main` commit on Windows 11.
- Record the full commit SHA in the image caption or adjacent page copy.
- Record whether the runtime is empty or seeded and identify the fixture used.
- Capture the native window at its 1240 × 760 default logical size at 100% scaling.
- Capture a second validation view at 150% Windows display scaling before publishing the asset.
- Show the current list-first interface without composited messages, fake provider output, or hidden warnings.
- Keep the turn boundary visible or state it beside the image: only the bounded Codex final-result path is implemented; Claude, streaming, approvals, interrupt, resume, fork, and edit authority remain gated.
- Do not show a provider response as promotion evidence without the external bundle: authenticated Windows and CLI versions, resolved native executable, observed sandbox mode, exact launch and feature inventory, redacted JSONL, pre/post worktree and Git-common-directory manifests, read and network attempts, process-tree counts after every exit path, and reopened SQLite turn/session/message state.
- Do not imply that AccessKit enabled is equivalent to completed NVDA or Narrator validation.
- Export responsive AVIF/WebP variants and keep the below-fold image at or below 250 KB.
- Give the image useful alt text and expose every annotation as real text outside the bitmap.

Until then, the durability trace on the homepage is an explanatory architecture visual, explicitly labeled as such.
