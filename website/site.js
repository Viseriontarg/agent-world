(()=>{"use strict";const header=document.querySelector('[data-header]'),menu=document.querySelector('[data-nav]'),toggle=document.querySelector('[data-menu-button]');const sync=()=>header?.classList.toggle('scrolled',scrollY>12);sync();addEventListener('scroll',sync,{passive:true});const close=()=>{menu?.classList.remove('open');toggle?.setAttribute('aria-expanded','false')};toggle?.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));menu?.classList.toggle('open',!open)});menu?.addEventListener('click',e=>{if(e.target instanceof HTMLAnchorElement)close()});addEventListener('keydown',e=>{if(e.key==='Escape')close()});const copy=document.querySelector('[data-copy]'),status=document.querySelector('[data-copy-status]');copy?.addEventListener('click',async()=>{const value=document.querySelector('#build-command')?.textContent?.trim();if(!value)return;try{await navigator.clipboard.writeText(value);copy.querySelector('span').textContent='Copied';if(status)status.textContent='Build command copied.';setTimeout(()=>copy.querySelector('span').textContent='Copy',1500)}catch{if(status)status.textContent='Copy failed. Select the command manually.'}});const items=[...document.querySelectorAll('.reveal')];if(matchMedia('(prefers-reduced-motion: reduce)').matches||!('IntersectionObserver'in window)){items.forEach(x=>x.classList.add('in'))}else{const io=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('in');io.unobserve(entry.target)}}),{threshold:.1,rootMargin:'0px 0px -7%'});items.forEach(x=>io.observe(x))}const year=document.querySelector('[data-year]');if(year)year.textContent=String(new Date().getFullYear())})();
